# voice-conscience mobile

Minimal Expo app that records audio and streams to the backend WebSocket.

Quick start:

1. Install dependencies:
   ```bash
   cd apps/mobile
   npm install
   ```

2. Run:
   ```bash
   npm start
   ```

3. Edit `config.ts` to set `BACKEND_URL` to your backend address.
